### Запуск docker-compose up


```
project.lo:8080                   Результаты запроса на выборку
localhost:8081                    phpMyAdmin | user:root password:secret
localhost:15672                   RabbitMq manager | user:admin password:admin
```